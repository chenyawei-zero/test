/*
const fs = require('fs');

fs.readFile('静夜思.txt', 'utf8', (err, data) => {
    if(err) {
        console.log(err)
    } else {
        console.log(data)
    }
})*/
console.log(__dirname)   // 路径
console.log(__filename)  // 路径加文件名
